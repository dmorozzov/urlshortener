version: "3.9"
services:
  redis:
    image: "redis"
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    entrypoint: >
      redis-server
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure